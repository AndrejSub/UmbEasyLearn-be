spring:
  data:
    mongodb:
      uri: mongodb+srv://asub:{password}@cluster0.gjveou6.mongodb.net
      database: Predmety
  application:
    name: subjectsms

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8090

